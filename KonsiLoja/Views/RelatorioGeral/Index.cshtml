@model IEnumerable<KonsiLoja.Models.RelatorioGeral>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Cliente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Contrato)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Vendedor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Cliente.ClienteId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Contrato.ContratoId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Vendedor.VendedorId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RelatorioGeralId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RelatorioGeralId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RelatorioGeralId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
